# Remove MCP/SDK jargon from user-facing pages

## Changes made

1. **core-usage/forks.mdx line 90** — replaced "MCP tools" with "tools". Kept "agent" unchanged since the rest of the file uses "agent" consistently (lines 16, 46, 105, 151, 154, 155, 166, 175).

2. **development/cli-reference.mdx line 360** — replaced "restricting which SDK tools the background fork can use" with "restricting which tools the background fork can use". Dropped "SDK".

## Skipped

- **changelog.mdx line 21** — the specified text ("Discord MCP tools are now automatically removed from `allowed-tools`") does not exist in the file. The content around line 21 covers reminders defaulting to background and fork prompt improvements. No matching text was found anywhere in the changelog. This edit was skipped to avoid incorrect changes.
