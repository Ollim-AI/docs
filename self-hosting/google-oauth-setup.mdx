---
title: "Google OAuth reference"
description: "Token lifecycle, auto-recovery, and scope management for Google OAuth."
---

Reference for how ollim-bot manages Google OAuth tokens after you've
completed the [setup steps](/getting-started/google-integration).

### What the setup creates

| Key | Value |
|-----|-------|
| **APIs enabled** | Google Tasks API, Google Calendar API, Gmail API |
| **OAuth scopes** | `tasks` (read/write), `calendar.events` (read/write), `gmail.readonly` |
| **Application type** | Desktop application (must be `"installed"` key, not `"web"`) |
| **Credentials file** | `~/.ollim-bot/state/credentials.json` |
| **Token file** | `~/.ollim-bot/state/token.json` (auto-created after `/google-auth`) |
| **Consent screen** | External, Testing mode (only listed test users can authenticate) |

## Token lifecycle

ollim-bot manages tokens automatically through `get_credentials()`
in `google/auth.py`:

| Scenario | Behavior |
|----------|----------|
| `token.json` exists, token valid | Used immediately — no network call |
| `token.json` exists, token expired | Refreshed via the refresh token, `token.json` updated |
| `token.json` missing | Run `/google-auth` to start a new OAuth flow |
| `credentials.json` missing | `/google-auth` shows setup instructions |

The token file at `~/.ollim-bot/state/token.json` stores the access token,
refresh token, and granted scopes. Do not edit it manually.

## Auto-recovery

When a Google token is revoked — by a password change, manual revocation in
Google account settings, or other means — the bot detects it automatically:

1. On the next Google API call, `get_credentials()` encounters a
   `RefreshError` with `invalid_grant`
2. The bot deletes the stale `token.json` and creates a marker file
3. The scheduler checks the marker on its 10-second polling loop and
   sends: "-# google auth revoked -- use /google-auth to reconnect."

You do not need to manually delete `token.json` or diagnose the issue. Run
`/google-auth` to reconnect.

## Adding or changing scopes

The OAuth scopes are defined in `google/auth.py`:

```python
SCOPES = [
    "https://www.googleapis.com/auth/tasks",
    "https://www.googleapis.com/auth/calendar.events",
    "https://www.googleapis.com/auth/gmail.readonly",
]
```

If you add a new Google service and need additional scopes:

1. Add the scope string to the `SCOPES` list
2. Delete `~/.ollim-bot/state/token.json`
3. Run `/google-auth` — the OAuth consent flow runs again with the
   updated scopes

<Note>
  Existing tokens do not automatically gain new scopes.
  You must delete `token.json` and re-consent whenever scopes change.
</Note>

## Next steps

<Columns cols={2}>
  <Card title="Google integration overview" icon="cloud" href="/integrations/google-overview">
    How the Google services work together.
  </Card>
  <Card title="Set up Google integration" icon="puzzle-piece" href="/getting-started/google-integration">
    Step-by-step setup for Google Tasks, Calendar, and Gmail.
  </Card>
  <Card title="Google Tasks" icon="list-check" href="/integrations/google-tasks">
    Manage tasks through the bot and CLI.
  </Card>
  <Card title="Google Calendar" icon="calendar" href="/integrations/google-calendar">
    Manage calendar events through the bot and CLI.
  </Card>
</Columns>
